# Git and GitHub Assignment Report

## Student

- GitHub account: `fa1dpip`
- Repository URL: <https://github.com/fa1dpip/fa1d>
- Repository visibility: Public

## Completed Requirements

1. Created a GitHub account and a public repository.
2. Uploaded one project to the `main` branch with multiple versions.
3. Added a `.gitignore` file with several ignore rules.
4. Added a `README.md` file with a project description.
5. Created a secondary branch for additional work.
6. Returned the secondary-branch changes back to `main`.

## Repository Evidence

- `.gitignore` includes multiple patterns:
  - `database/*.sqlite`
  - `database/*.sqlite-*`
  - `tests/tmp/`
  - `report/build-report-pdf.ps1`
- `README.md` describes the project structure, features, and local run steps.
- GitHub commit history contains multiple milestone versions:
  - `Version 1: initialize repository for PHP user management task`
  - `Version 2: add project runtime configuration`
  - `Version 3: add OOP domain structure for user roles and sessions`
  - `Version 4: add repositories and authentication services`
  - `Version 5: add web interface and automated integration test`
  - `Version 6: add finished interface styling`
  - `Version 7: add submission report sources`

## Branch Workflow

- Secondary branch name: `assignment-report`
- Purpose: add report materials for submission
- Result: the secondary-branch work was transferred back to `main`

## Screenshots To Attach

Add the following screenshots before submitting to Moodle:

1. GitHub profile or account page showing the logged-in account.
2. Repository main page with the public URL visible.
3. Commit history page showing multiple versions in `main`.
4. Repository file list showing `.gitignore` and `README.md`.
5. Branch selector showing the secondary branch.
6. `main` branch after the secondary-branch changes were returned.

## Conclusion

The repository meets the assignment requirements for Git and GitHub: version history in `main`, a `.gitignore`, a `README.md`, a secondary branch, and returning the secondary-branch changes back to `main`.
